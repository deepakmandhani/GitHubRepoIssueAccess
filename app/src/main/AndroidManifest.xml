<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sigtuple.com.githubrepoaccess">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:name="sigtuple.com.sigtuple.SigTupleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name="sigtuple.com.sigtuple.activities.WebViewActivity" />
        <activity android:name="sigtuple.com.sigtuple.activities.GitHubRepoAccessActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
